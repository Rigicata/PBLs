@model IEnumerable<PBLCopos.Models.Bebedouro>

@{
    ViewBag.Title = "BebedourosVazios";
}

<h2>BebedourosVazios</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.Localizacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StatusCopo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.Localizacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StatusCopo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BebedouroId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BebedouroId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BebedouroId })
        </td>
    </tr>
}

</table>
